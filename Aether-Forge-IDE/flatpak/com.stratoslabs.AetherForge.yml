id: com.xytrolabs.IndentForge
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
command: indent-forge
finish-args:
  - --share=ipc
  - --socket=wayland
  - --socket=x11
  - --socket=pulseaudio
  - --share=network
  - --device=dri
  - --filesystem=home
  - --talk-name=org.freedesktop.Notifications
modules:
  - name: indent-forge
    buildsystem: simple
    build-options:
      append-path: /usr/lib/sdk/rust-stable/bin
      env:
        CARGO_HOME: /run/build/indent-forge/cargo
        WEBKIT_DISABLE_COMPOSITING_MODE: '1'
    build-commands:
      - cargo install tauri-cli --version "^2"
      - cd src-tauri && cargo build --release
      - install -Dm755 target/release/indent-forge /app/bin/indent-forge
      - install -Dm644 ../share/metainfo/com.xytrolabs.IndentForge.metainfo.xml /app/share/metainfo/com.xytrolabs.IndentForge.metainfo.xml
      - install -Dm644 ../share/icons/hicolor/256x256/apps/com.xytrolabs.IndentForge.png /app/share/icons/hicolor/256x256/apps/com.xytrolabs.IndentForge.png
      - install -Dm644 ../share/applications/com.xytrolabs.IndentForge.desktop /app/share/applications/com.xytrolabs.IndentForge.desktop
    sources:
      - type: dir
        path: ../../
