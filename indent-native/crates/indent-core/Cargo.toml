[package]
name = "indent-core"
version = "1.0.0"
edition = "2021"
description = "Indent language AST, types, and compiler core — shared by CLI, LSP, and IDE"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
