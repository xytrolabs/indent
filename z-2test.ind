var username string = ask("What is your name?")
var names dict[string]bool = {}
isnames(username) := true in names
say "Hello, " + username + "!"

fun isnames key value bool result boolean
    if keys contains key and values of the list associated with that key are all booleans
        give True
    otherwise
        give False